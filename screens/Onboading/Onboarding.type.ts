import { NativeStackScreenProps } from "@react-navigation/native-stack";
import { RootStack } from "../../App.type";

export type OnboardingScreenType = NativeStackScreenProps<
  RootStack,
  "Onboarding"
>;
